services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: nexacore-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"   # REST API and dashboard at http://localhost:6333/dashboard
      - "6334:6334"   # gRPC
    volumes:
      - ./qdrant_storage:/qdrant/storage
